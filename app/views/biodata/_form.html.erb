<%= form_for(@biodatum) do |f| %>
  <% if @biodatum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@biodatum.errors.count, "error") %> prohibited this biodatum from being saved:</h2>

      <ul>
      <% @biodatum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nama %><br>
    <%= f.text_field :nama %>
  </div>
  <div class="field">
    <%= f.label :ttl %><br>
    <%= f.text_area :ttl %>
  </div>
  <div class="field">
    <%= f.label :alamat %><br>
    <%= f.text_area :alamat %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
